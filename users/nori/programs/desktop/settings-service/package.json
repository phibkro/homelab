{
  "name": "@phibkro/nori-desktop-settings-service",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts --compile --outfile dist/nori-desktop-settings && bun build src/rice-saved-command.ts --compile --outfile dist/rice-saved-command",
    "check": "bun run typecheck && bun test",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "effect": "4.0.0-rc.108"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "26.1.1",
    "typescript": "7.0.2"
  }
}
