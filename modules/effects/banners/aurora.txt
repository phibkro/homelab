TODO: draft aurora ASCII art here.

This file is dumped as-is at the top of the MOTD on the host whose
codename is "aurora". Keep it under ~12 lines tall so it doesn't
push the live stats off-screen. No newline-prefix needed; rust-motd
adds spacing.
