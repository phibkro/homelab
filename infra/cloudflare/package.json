{
  "name": "@homelab/infra-cloudflare",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "login": "alchemy login",
    "login:configure": "alchemy login --configure",
    "deploy": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-main -- alchemy deploy",
    "deploy:projects": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-projects -- alchemy deploy projects.alchemy.run.ts",
    "deploy:projects-relay": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-projects-relay -- alchemy deploy projects-relay.alchemy.run.ts",
    "deploy:dev-share": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-dev-share -- alchemy deploy dev-share.alchemy.run.ts",
    "plan": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-main -- alchemy plan",
    "plan:projects": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-projects -- alchemy plan projects.alchemy.run.ts",
    "plan:projects-relay": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-projects-relay -- alchemy plan projects-relay.alchemy.run.ts",
    "plan:dev-share": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-dev-share -- alchemy plan dev-share.alchemy.run.ts",
    "destroy": "secretspec run --file ../../secretspec.toml --profile workstation --scope cloudflare-main -- alchemy destroy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform-bun": "4.0.0-rc.108",
    "@effect/platform-node": "4.0.0-rc.108",
    "alchemy": "2.0.0-beta.45",
    "effect": "4.0.0-rc.108"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
