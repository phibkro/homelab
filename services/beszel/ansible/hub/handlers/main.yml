---
- name: Reload systemd after Beszel changes
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart Beszel hub
  ansible.builtin.systemd_service:
    name: beszel.service
    state: restarted
