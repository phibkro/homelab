---
- name: Reload systemd after Vector changes
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart Vector
  ansible.builtin.systemd_service:
    name: vector.service
    state: restarted
