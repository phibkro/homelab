---
- name: Reload systemd manager after heartbeat unit changes
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart heartbeat timer after schedule changes
  ansible.builtin.systemd_service:
    name: heartbeat.timer
    state: restarted
    daemon_reload: true
